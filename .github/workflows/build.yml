name: Build Releases

on:
  push:
    tags:
      - 'v*'
    branches:
      - 'release/*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: System info
        shell: pwsh
        run: |
          Write-Host "Node $(node --version)  npm $(npm --version)"
          Write-Host "Working dir: $(Get-Location)"

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build --workspace=frontend

      - name: Build backend
        run: npm run build --workspace=backend

      - name: Prepare backend node_modules for bundling
        run: node scripts/prep-backend-bundle.js

      - name: Build Windows installer
        run: npm run build:win --workspace=electron
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Prevent electron-builder from trying to publish
          EP_DRAFT: true
          EP_PRE_RELEASE: false

      - name: Find and rename installer
        shell: pwsh
        run: |
          $files = Get-ChildItem "electron/out" -Filter "*.exe" -ErrorAction SilentlyContinue
          if (-not $files) {
            Write-Host "Contents of electron/out:"
            Get-ChildItem "electron/out" -ErrorAction SilentlyContinue
            Write-Error "No .exe found in electron/out — electron-builder may have failed"
            exit 1
          }
          $src = $files | Select-Object -First 1
          Write-Host "Packaging: $($src.Name)"
          Copy-Item $src.FullName "electron/out/TradeEdge-Setup-x64.exe"

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: electron/out/TradeEdge-Setup-x64.exe
          retention-days: 3

  build-android:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build --workspace=frontend

      - name: Add Android platform
        working-directory: frontend
        run: npx cap add android 2>/dev/null || true

      - name: Sync Capacitor
        working-directory: frontend
        run: npx cap sync android

      - name: Make gradlew executable
        working-directory: frontend/android
        run: chmod +x gradlew

      - name: Build debug APK
        working-directory: frontend/android
        run: ./gradlew assembleDebug

      - name: Rename APK
        run: cp frontend/android/app/build/outputs/apk/debug/app-debug.apk TradeEdge.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: TradeEdge.apk
          retention-days: 3

  create-release:
    needs: [build-windows, build-android]
    runs-on: ubuntu-latest
    if: always() && needs.build-windows.result == 'success'
    steps:
      - uses: actions/checkout@v4

      - name: Download Windows installer
        uses: actions/download-artifact@v4
        with:
          name: windows-installer
          path: dist/

      - name: Download Android APK
        uses: actions/download-artifact@v4
        with:
          name: android-apk
          path: dist/
        continue-on-error: true

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          REF="${GITHUB_REF}"
          if [[ "$REF" == refs/tags/* ]]; then
            TAG="${REF#refs/tags/}"
          elif [[ "$REF" == refs/heads/release/* ]]; then
            TAG="${REF#refs/heads/release/}"
          else
            TAG="v1.0.0"
          fi

          FILES=""
          [ -f dist/TradeEdge-Setup-x64.exe ] && FILES="$FILES dist/TradeEdge-Setup-x64.exe"
          [ -f dist/TradeEdge.apk ] && FILES="$FILES dist/TradeEdge.apk"

          gh release create "$TAG" \
            --target "${GITHUB_SHA}" \
            --title "TradeEdge $TAG" \
            --notes "## TradeEdge $TAG

          ### Downloads
          | Platform | File | Notes |
          |---|---|---|
          | Windows 10/11 (64-bit) | \`TradeEdge-Setup-x64.exe\` | Run installer — no admin rights needed |
          | Android 7+ | \`TradeEdge.apk\` | Enable \"Install unknown apps\", tap APK to install |

          ### Quick Start
          Both apps launch with **demo market data** immediately — no account, backend, or internet required.

          ### Connect Android to Live Data
          1. Run \`npm run dev\` on your PC (or deploy backend to Render/Railway)
          2. Open app → tap ⚙ → enter your backend URL (e.g. \`http://192.168.1.x:3001\`)
          3. Tap **Save & Reload**

          See [INSTALL.md](https://github.com/${{ github.repository }}/blob/main/INSTALL.md) for full setup instructions." \
            $FILES
